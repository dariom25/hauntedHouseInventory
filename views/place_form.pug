extends layout

block content
    h2 #{title}

    form(method="POST")
        div.form-group
            label(for="name") Name:
            input#name(type="text", placeholder="Name of Place" name="name" required)
        button(type="submit") Submit
    if errors
        ul 
            for error in errors 
                li!= error.msg